Host stash.*
  PreferredAuthentications publickey
  IdentityFile /Users/cliang/.ssh/id_rsa

Host *
  User cliang
  ControlMaster auto
  ForwardAgent yes
  IdentityFile ~/.ssh/id_rsa

Host prod
  IdentityFile /Users/cliang/.ssh/id_rsa
  Hostname 54.86.185.200
  StrictHostKeyChecking no
 
Host prod+*
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no
 
Host stage
  Hostname 54.86.15.31
  StrictHostKeyChecking no
 
Host stage+*
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no
 
Host dev
  Hostname 54.84.11.127
 
Host dev+*
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no
 
Host sin
  Hostname 52.220.20.0

Host sin+*
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no

Host aus
  Hostname 52.65.172.49

Host aus+*
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no

Host eu
  IdentityFile /Users/cliang/.ssh/id_rsa
  Hostname 35.157.154.33

Host eu+*
  IdentityFile /Users/cliang/.ssh/id_rsa
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no

Host gov
  IdentityFile /Users/cliang/.ssh/id_rsa
  Hostname learn-us-gov-west-1-jumphost.bbpd.io
  StrictHostKeyChecking no

Host gov+*
  IdentityFile /Users/cliang/.ssh/id_rsa
  ControlMaster auto
  ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
  ControlPath ~/.ssh/ssh-control_%r@%h:%p
  StrictHostKeyChecking no

Host pg
 Hostname 54.156.242.20

Host pg+*
 ControlMaster auto
 ProxyCommand ssh -T -a $(echo %h |cut -d+ -f1) nc $(echo %h |cut -d+ -f2) %p 2>/dev/null
 ControlPath ~/.ssh/ssh-control_%r@%h:%p
 StrictHostKeyChecking no
