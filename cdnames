# Shared cd shortcuts go here. Keep portable entries inline and let the
# office profile layer load sensitive ones from office/cdnames.
# Example:
#   proj=~/projects/my-app

ZSH_OFFICE_CDNAMES="${ZSH_OFFICE_CDNAMES:-${ZDOTDIR:-$HOME/.zsh}/office/cdnames}"
if [[ ${ZSH_PROFILE:-} == office && -r "$ZSH_OFFICE_CDNAMES" ]]; then
    source "$ZSH_OFFICE_CDNAMES"
fi
