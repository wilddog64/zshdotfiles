[user]
    name = CK
    email = chengkai.liang@gigamon.com
[alias]
    co = checkout
    st = "!f() { git status --ignore-submodules --short; }; f"
    diffc = diff --cached
    merge-with-no-ff = merge --no-ff
    merge-master = merge --no-ff master
    merge-develop = merge --no-ff develop
    aliases = config --get-regexp ^alias\\.*$
    pullrb = pull --rebase
    outfiles = "!f() { git diff --name-status ..$1;  }; f"
    tip = log --date relative --pretty='%Cgreen %h %an %ai %s' -1
    todayschanges = "!f() { git whatchanged --oneline --since=yesterday | grep -v '^:'; }; f"
    push-tag = "!f() { git push; git push --tag; }; f"
    push-all = "!f() { git push -u --all; git push --tag; }; f"
    timeline = log --topo-order --date relative --pretty='%Cgreen %h %an %ai %s' --graph
[branch]
    autosetuprebase            = always
[color]
    diff = always
    ui   = on
[merge]
    sumary = true
    tool = p4merge
[mergetool "p4merge"]
    cmd  = p4merge \
        "$PWD/$BASE" \
        "$PWD/$LOCAL" \
        "$PWD/$REMOTE" \
        "$PWD/$MERGED"
    keppBackup     = false
    trustExistCode = false
[push]
    default = upstream
[difftool "sourcetree"]
    cmd = opendiff \"$LOCAL\" \"$REMOTE\"
    path =
[mergetool "sourcetree"]
    cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
    trustExitCode = true
[core]
    excludesfile = /Users/cliang/.gitignore_global
    pager        = cat
    autocrlf     = false
    safecrlf     = false
    eol          = crlf
    filemode     = false
    ignorecase   = false
    editor       = /usr/local/bin/nvim
[hub]
    protocol = ssh
[credential]
    helper = osxkeychain

[bulkworkspaces]
    puppet = /Users/cliang/src/gitrepo/gigamon/gigamon-puppet-repo
    ansible = /Users/cliang/src/gitrepo/personal/ansible
